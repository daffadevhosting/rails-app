<!DOCTYPE html>
<html lang="id-ID" class="scroll-smooth dark">
<head>
  <title><%= content_for(:title) || "Rails App" %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">

  <script src="https://cdn.tailwindcss.com"></script>

  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            poppins: ['Poppins', 'sans-serif'],
          },
        }
      }
    }
  </script>

  <%= yield :head %>

  <%= javascript_importmap_tags %>

    <style>
        body {
            font-family: 'Poppins', sans-serif;
        }
        .cta-glow {
            box-shadow: 0 0 15px 2px rgba(124, 58, 237, 0.4), 0 0 30px -5px rgba(99, 102, 241, 0.3);
        }
    </style>
<script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>

<script src="https://app.sandbox.midtrans.com/snap/snap.js" data-client-key="<%= ENV["MIDTRANS_CLIENT_KEY"] %>"></script>
  </head>

  <body class="bg-slate-900 text-white overflow-x-hidden text-slate-200">
      <%= yield %>

    <footer class="bg-slate-900 border-t border-slate-800 py-10">
        <div class="container mx-auto px-6 text-center text-slate-400">
            <p>&copy; 2025 Imagina. Dibuat dengan imajinasi.</p>
        </div>
    </footer>
<script src="https://cdn.jsdelivr.net/npm/@fingerprintjs/fingerprintjs@3/dist/fp.min.js"></script>
<script>
  FingerprintJS.load().then(fp => {
    fp.get().then(result => {
      document.cookie = "fingerprint=" + result.visitorId + "; path=/; max-age=31536000";
    });
  });
</script>
  </body>
</html>
